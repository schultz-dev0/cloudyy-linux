-- Cloud Center user override file for lookandfeel configuration.
-- @cloud-center-state = {"decoration:shadow:range": "30"}

require("source.lookandfeel")

hl.config({
    decoration = {
        shadow = {
            range = 30,
        },
    },
})
