-- Input: keyboard layout, mouse sensitivity
-- Sources: source/input.conf + user-configs/user_input.conf (identical here)

hl.config({
    input = {
        kb_layout  = "gb,ru",
        kb_options = "grp:alt_shift_toggle",
        follow_mouse = 1,
        sensitivity  = 0,
    },
})
