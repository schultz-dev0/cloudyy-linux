# =============================================================================
# Shell stack autostart — managed by ~/cloudyy-linux/install/apply-shell-stack.sh
# Edit ~/cloudyy-linux/install/shell-stack.conf to change profiles or add slots.
# Re-run:   bash ~/cloudyy-linux/install/select-shell-stack.sh
# =============================================================================
# Active profile: quickshell

# --- bar ---   (impl: quickshell)
exec-once = qs -d

# --- notifications ---   (impl: quickshell)

# --- launcher ---   (impl: rofi)

# --- volume_osd ---   (impl: swayosd)

# --- app_menu ---   (slot reserved)

# --- power_menu ---   (impl: rofi)

