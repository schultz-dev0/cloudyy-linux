#!/bin/bash
# Hyprland Dependencies Configuration File
# Based on actual installed packages from: pacman -Qetq
# Edit this file to add or remove packages for your setup
# Lines starting with # are comments and will be ignored
# Empty lines are ignored

# ============================================================================
# CORE HYPRLAND PACKAGES
# ============================================================================
# These are essential for Hyprland to function
CORE_PACKAGES=(
  "hyprland"                    # Hyprland compositor (if not listed, it's a dependency)
  "hypridle"                    # Idle daemon for Hyprland
  "hyprlock"                    # Screen locker
  "hyprpolkitagent"             # Polkit authentication agent
  "xdg-desktop-portal-hyprland" # Screensharing and more
  "qt5-wayland"                 # Qt5 Wayland support
  "qt6-wayland"                 # Qt6 Wayland support
  "wlroots-nvidia"              # Wlroots with NVIDIA patches
)

# ============================================================================
# SYSTEM BASE & BOOT
# ============================================================================
SYSTEM_PACKAGES=(
  #"base"         # Arch base system
  #"efibootmgr"   # EFI boot manager
  #"limine"       # Bootloader
  #"intel-ucode"  # Intel CPU microcode
  #"sof-firmware" # Sound Open Firmware
)

# ============================================================================
# GPU DRIVERS
# ============================================================================
# Uncomment the section that matches your GPU

# --- NVIDIA GPU (your current setup) ---
GPU_PACKAGES=(
  "nvidia-open-dkms" # NVIDIA open kernel modules
  "wlroots-nvidia"   # Wlroots with NVIDIA patches
)

# --- Intel iGPU (integrated graphics) ---
# Uncomment these if you're using Intel integrated graphics instead
# GPU_PACKAGES=(
#     "mesa"                              # OpenGL implementation
#     "lib32-mesa"                        # 32-bit OpenGL support
#     "vulkan-intel"                      # Vulkan support for Intel
#     "lib32-vulkan-intel"                # 32-bit Vulkan support
#     "intel-media-driver"                # Hardware video acceleration
#     "libva-intel-driver"                # VA-API support (older hardware)
# )

# --- AMD GPU ---
# Uncomment these if you're using AMD graphics
# GPU_PACKAGES=(
#     "mesa"                              # OpenGL implementation
#     "lib32-mesa"                        # 32-bit OpenGL support
#     "vulkan-radeon"                     # Vulkan support for AMD
#     "lib32-vulkan-radeon"               # 32-bit Vulkan support
#     "libva-mesa-driver"                 # VA-API support
#     "lib32-libva-mesa-driver"           # 32-bit VA-API support
# )

# ============================================================================
# TERMINAL & SHELL
# ============================================================================
TERMINAL_PACKAGES=(
  "kitty"                       # Terminal emulator
  "zsh-autosuggestions-git"     # ZSH autosuggestions
  "zsh-syntax-highlighting-git" # ZSH syntax highlighting
)

# ============================================================================
# AUDIO
# ============================================================================
# Note: PipeWire packages are likely installed as dependencies
AUDIO_PACKAGES=(
  "pavucontrol" # Volume control GUI
  "cava"        # Console audio visualizer
)

# ============================================================================
# INTERFACE COMPONENTS
# ============================================================================
INTERFACE_PACKAGES=(
  "waybar"           # Status bar
  "waybar-mpris-git" # MPRIS widget for waybar
  "mako"             # Notification daemon
  "swaync"           # Alternative notification center
  "swayosd"          # On-screen display for volume/brightness
  "wlogout"          # Logout menu
  "wlsunset"         # Day/night gamma adjustments
  "yad"              # Yet Another Dialog
)

# ============================================================================
# APPLICATION LAUNCHERS & TOOLS
# ============================================================================
LAUNCHER_PACKAGES=(
  "swww"
  "rofi"

)

# ============================================================================
# FILE MANAGEMENT
# ============================================================================
FILE_PACKAGES=(
  "thunar"         # File manager
  "stow"           # Symlink manager (dotfiles)
  "gparted"        # Partition editor
  "isoimagewriter" # ISO image writer
  "loupe"          # Image viewer
)

# ============================================================================
# SCREENSHOT & SCREEN RECORDING
# ============================================================================
SCREENSHOT_PACKAGES=(
  "hyprcap" # Hyprland screen capture
)

# Note: gpu-screen-recorder and obs-studio moved to OPTIONAL_PACKAGES
# since they're hardware-specific or may not be needed by everyone

# ============================================================================
# CLIPBOARD & TEXT
# ============================================================================
CLIPBOARD_PACKAGES=(
  "cliphist" # Clipboard manager
)

# ============================================================================
# SYSTEM UTILITIES
# ============================================================================
UTILITY_PACKAGES=(
  "brightnessctl"        # Brightness control
  "bluez-utils"          # Bluetooth utilities
  "blueman"              # Bluetooth manager GUI
  "bluetui"              # Bluetooth TUI
  "nm-connection-editor" # NetworkManager GUI
  "iwd"
  "wifitui"        # WiFi TUI
  "reflector"      # Pacman mirror updater
  "snapper"        # Snapshot manager
  "zram-generator" # ZRAM configuration
)

# ============================================================================
# MONITORING & SYSTEM INFO
# ============================================================================
MONITORING_PACKAGES=(
  "btop"      # Resource monitor
  "fastfetch" # System info
  #"nct6687d-dkms-git" # Nuvoton NCT6687-D kernel module
)

# ============================================================================
# FONTS
# ============================================================================
FONT_PACKAGES=(
  "ttf-jetbrains-mono-nerd" # JetBrains Mono Nerd Font
  "otf-monocraft"           # Monocraft font
)

# ============================================================================
# DEVELOPMENT TOOLS
# ============================================================================
DEV_PACKAGES=(
  "git-filter-repo" # Git filter tool
  "code"            # VS Code
  "neovim"          # Neovim text editor
  "nano"            # Nano text editor
  "pycharm"         # PyCharm IDE
  "pnpm"            # Node package manager
  "unzip"           # Archive utility
  "rust"
)

# ============================================================================
# WEB BROWSERS
# ============================================================================
BROWSER_PACKAGES=(
  "firefox" # Firefox browser
  #"chromium"        # Chromium browser
  "python-pywalfox" # Pywal integration for Firefox
)

# ============================================================================
# APPLICATIONS
# ============================================================================
APP_PACKAGES=(
  "vesktop"      # Vesktop (Discord client)
  "obsidian"     # Note-taking app
  "mpv"          # Video player
  "keypunch-git" # Typing practice
)

# ============================================================================
# OPTIONAL APPLICATIONS
# ============================================================================
# Gaming, streaming, and media apps - uncomment what you want
OPTIONAL_PACKAGES=(
  # --- Gaming ---
  # "steam"                           # Gaming platform

  # --- Music Streaming ---
  # "spotify"                         # Spotify
  # "spotify-qt"                      # Spotify Qt client
  # "spicetify-cli"                   # Spotify customization

  # --- Video & Streaming ---
  # "obs-studio"                      # OBS Studio for streaming/recording
  # "gpu-screen-recorder"             # GPU-accelerated screen recorder (NVIDIA/AMD only)

  # --- Other Optional Apps ---
  # "gimp"                            # Image editor
  # "inkscape"                        # Vector graphics
  # "blender"                         # 3D creation suite
  # "libreoffice-fresh"               # Office suite
  # "transmission-gtk"                # Torrent client
  # "qbittorrent"                     # Alternative torrent client
  # "vlc"                             # Media player
  # "audacity"                        # Audio editor
  # "coolercontrol"
  # "syncthing"
)

# ============================================================================
# THEMES & APPEARANCE
# ============================================================================
THEME_PACKAGES=(
  "adw-gtk-theme" # Adwaita GTK theme
  "orchis-theme"  # Orchis theme
  "nwg-look"      # GTK settings editor
  "matugen"       # Material color generation
  "waypaper"      # Wallpaper manager
  "icat"          # Image viewer for terminal
)

# ============================================================================
# POLKIT AGENTS
# ============================================================================
POLKIT_PACKAGES=(
  "polkit-gnome" # GNOME polkit agent
  # hyprpolkitagent is in CORE_PACKAGES
)

# ============================================================================
# AUR HELPER
# ============================================================================
AUR_HELPER_PACKAGES=(
  "yay-bin"       # AUR helper
  "yay-bin-debug" # Debug symbols for yay
)

# ============================================================================
# AUR PACKAGES
# ============================================================================
# These are from AUR (need yay or another AUR helper)
AUR_PACKAGES=(
  #"brave-bin"
  "bluetui"
  "coolercontrol"
  # "gpu-screen-recorder"             # Moved to OPTIONAL (NVIDIA/AMD only)
  "hyprcap"
  "hypridle"
  "hyprlock"
  "hyprpolkitagent"
  "icat"
  "isoimagewriter"
  "keypunch-git"
  "matugen"
  "nct6687d-dkms-git"
  "nwg-look"
  # "nvidia-open-dkms"                # In GPU_PACKAGES now
  "orchis-theme"
  "otf-monocraft"
  "python-pywalfox"
  "sad"
  # "spicetify-cli"                   # Moved to OPTIONAL
  # "spotify-qt"                      # Moved to OPTIONAL
  "swaync"
  "swayosd"
  "vesktop"
  "waybar-mpris-git"
  "waypaper"
  "wifitui"
  # "wlroots-nvidia"                  # In GPU_PACKAGES now
  "yay-bin"
  "yay-bin-debug"
  "zsh-autosuggestions-git"
  "zsh-syntax-highlighting-git"
  "nautilus"
)

# ============================================================================
# PYTHON PACKAGES (if any were installed via pip)
# ============================================================================
PYTHON_PACKAGES=(
  # Add any pip packages here if needed
)

# ============================================================================
# PACKAGE GROUPS
# ============================================================================
# Define groups for easier selective installation

# Minimal installation (core Hyprland only)
MINIMAL_GROUP=(
  "${CORE_PACKAGES[@]}"
  "${SYSTEM_PACKAGES[@]}"
  "${GPU_PACKAGES[@]}"
  "${TERMINAL_PACKAGES[@]:0:1}"  # Just kitty
  "${INTERFACE_PACKAGES[@]:0:3}" # waybar, mako, wlogout
  "${CLIPBOARD_PACKAGES[@]}"
  "${UTILITY_PACKAGES[@]:0:2}" # brightnessctl, bluez-utils
)

# Standard installation (most common packages)
STANDARD_GROUP=(
  "${CORE_PACKAGES[@]}"
  "${SYSTEM_PACKAGES[@]}"
  "${GPU_PACKAGES[@]}"
  "${TERMINAL_PACKAGES[@]}"
  "${AUDIO_PACKAGES[@]}"
  "${INTERFACE_PACKAGES[@]}"
  "${FILE_PACKAGES[@]}"
  "${SCREENSHOT_PACKAGES[@]}"
  "${CLIPBOARD_PACKAGES[@]}"
  "${UTILITY_PACKAGES[@]}"
  "${MONITORING_PACKAGES[@]}"
  "${FONT_PACKAGES[@]}"
  "${DEV_PACKAGES[@]}"
  "${BROWSER_PACKAGES[@]}"
  "${THEME_PACKAGES[@]}"
  "${POLKIT_PACKAGES[@]}"
)

# Full installation (everything)
FULL_GROUP=(
  "${CORE_PACKAGES[@]}"
  "${SYSTEM_PACKAGES[@]}"
  "${GPU_PACKAGES[@]}"
  "${TERMINAL_PACKAGES[@]}"
  "${AUDIO_PACKAGES[@]}"
  "${INTERFACE_PACKAGES[@]}"
  "${FILE_PACKAGES[@]}"
  "${SCREENSHOT_PACKAGES[@]}"
  "${CLIPBOARD_PACKAGES[@]}"
  "${UTILITY_PACKAGES[@]}"
  "${MONITORING_PACKAGES[@]}"
  "${FONT_PACKAGES[@]}"
  "${DEV_PACKAGES[@]}"
  "${BROWSER_PACKAGES[@]}"
  "${APP_PACKAGES[@]}"
  "${THEME_PACKAGES[@]}"
  "${POLKIT_PACKAGES[@]}"
  "${AUR_HELPER_PACKAGES[@]}"
)

# ============================================================================
# CUSTOM PACKAGES
# ============================================================================
# Add any additional packages here that aren't in the above lists
CUSTOM_PACKAGES=(
  "sad" # CLI search and replace tool
)

# ============================================================================
# NOTES
# ============================================================================
# Some packages from your list may be installed as dependencies:
# - hyprland (might be a dependency)
# - pipewire, wireplumber, etc. (audio dependencies)
# - qt5/qt6-wayland (Wayland dependencies)
#
# This configuration is based on your explicitly installed packages.
# Dependencies will be installed automatically by pacman.
