# /etc/mkinitcpio.conf.d/cloudyy.conf
# keymap must stay before encrypt so passphrase entry matches layout.
HOOKS=(base udev plymouth keyboard autodetect microcode modconf kms keymap consolefont block encrypt filesystems fsck cloudyy-coldboot)
FILES=(/etc/vconsole.conf)
